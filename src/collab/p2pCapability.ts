// Moved to @peerly/core so other apps can share it; re-exported here so
// existing imports keep working. src/types re-exports the P2pCapability type.
export { probeP2pCapability } from '@peerly/core'
